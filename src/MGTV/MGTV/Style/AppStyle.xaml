<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" 
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:MGTV.Style">

    <!--Common-->

    <Style x:Key="textBlockBase" TargetType="TextBlock">

    </Style>

    <Style x:Key="RootGrid" TargetType="Grid">
        <Setter Property="Background">
            <Setter.Value>
                <ImageBrush ImageSource="../assets/background.png" AlignmentX="Left" AlignmentY="Top" Stretch="None"/>
            </Setter.Value>
        </Setter>
    </Style>

    <!--ItemTile-->
    <Style x:Key="ItemTile_Title_Text" TargetType="TextBlock" BasedOn="{StaticResource textBlockBase}">
        <Setter Property="VerticalAlignment" Value="Bottom"/>
        <Setter Property="FontSize" Value="18"/>
        <Setter Property="Foreground" Value="White"/>
        <Setter Property="TextTrimming" Value="CharacterEllipsis"/>
        <Setter Property="TextWrapping" Value="NoWrap"/>
        <Setter Property="Margin" Value="0, 0, 0, 19"/>
    </Style>

    <Style x:Key="ItemTile_Intro_Text" TargetType="TextBlock" BasedOn="{StaticResource textBlockBase}">
        <Setter Property="FontSize" Value="14"/>
        <Setter Property="Foreground" Value="White"/>
        <Setter Property="VerticalAlignment" Value="Bottom"/>
        <Setter Property="TextTrimming" Value="CharacterEllipsis"/>
        <Setter Property="TextWrapping" Value="NoWrap"/>
        <Setter Property="Margin" Value="0, 0, 0, 3"/>
    </Style>

</ResourceDictionary>
