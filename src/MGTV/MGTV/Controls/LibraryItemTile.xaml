<UserControl
    x:Class="MGTV.Controls.LibraryItemTile"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:MGTV.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="124"
    d:DesignWidth="448">

    <Grid Name="root" Background="#2c2c2c">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Border Grid.Column="0">
            <Border.Background>
                <ImageBrush ImageSource="{Binding ImageUrl}" AlignmentX="Center" AlignmentY="Center" Stretch="UniformToFill"/>
            </Border.Background>
        </Border>

        <Grid Grid.Column="1" Margin="25, 0, 0, 0">
            <TextBlock Text="{Binding Title}" Foreground="White" FontSize="24" Margin="0, 15, 0, 0" TextTrimming="CharacterEllipsis"/>
            <TextBlock Text="{Binding Intro}" Foreground="#e0e0e0" Margin="0, 50, 0, 0" TextTrimming="CharacterEllipsis" TextWrapping="Wrap"/>
            <TextBlock Text="{Binding PlayCout}" Foreground="#e0e0e0" VerticalAlignment="Bottom" Margin="0, 0, 0, 10" TextTrimming="CharacterEllipsis"/>
        </Grid>
    </Grid>
</UserControl>
