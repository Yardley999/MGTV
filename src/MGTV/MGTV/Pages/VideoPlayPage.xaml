<Page
    x:Class="MGTV.Pages.VideoPlayPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:MGTV.Pages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Style="{StaticResource RootGrid}" x:Name="root">
        <MediaElement x:Name="player" Margin="0, 0, 0, 64"/>

        <Grid x:Name="header" VerticalAlignment="Top" Background="Black">
            <ListBox x:Name="playListBox" ItemsSource="{Binding PlayLists}" Background="Transparent">
                <ListBox.ItemsPanel>
                    <ItemsPanelTemplate>
                        <StackPanel Orientation="Horizontal"/>
                    </ItemsPanelTemplate>
                </ListBox.ItemsPanel>
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <Grid Background="Transparent" Tapped="PlayListItem_Tapped">
                            <Border Background="#313131" CornerRadius="5" Visibility="{Binding IsPlaying, Converter={StaticResource AnythingToVisibilityConverter}, ConverterParameter=neg}">
                                <TextBlock FontSize="19" Foreground="White" Text="{Binding Name}" Margin="10, 5"/>
                            </Border>
                            <Border Background="#f06000" CornerRadius="5" Visibility="{Binding IsPlaying, Converter={StaticResource AnythingToVisibilityConverter}}">
                                <TextBlock FontSize="19" Foreground="White" Text="{Binding Name}" Margin="10, 5"/>
                            </Border>
                        </Grid>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </Grid>

        <Grid x:Name="StatusBar" VerticalAlignment="Bottom" Height="64" Background="#ee090806">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="213"/>
                <ColumnDefinition/>
                <ColumnDefinition Width="189"/>
            </Grid.ColumnDefinitions>

            <StackPanel Orientation="Horizontal" Grid.Column="0" HorizontalAlignment="Right" VerticalAlignment="Center" Margin="0,17,10,17" Height="30" Width="130">
                <Image x:Name="playButton" Source="../Assets/play.png" Stretch="None" VerticalAlignment="Center" Tapped="playControl_Tapped"/>
                <Image x:Name="PauseButton" Source="../Assets/pause.png" Visibility="Collapsed" Stretch="None" VerticalAlignment="Center" Tapped="playControl_Tapped"/>
                <Image Source="../Assets/Next.png" Stretch="None" Margin="5, 0, 0, 0" VerticalAlignment="Center" Tapped="next_tapped"/>
                <TextBlock x:Name="currentPosition" Text="00:00" Foreground="White" Margin="10, 0, 0, 0" VerticalAlignment="Center"/>
            </StackPanel>

            <Slider x:Name="progress" Grid.Column="1" VerticalAlignment="Center" Height="44" Margin="0,10"/>

            <StackPanel Orientation="Horizontal" Grid.Column="2" VerticalAlignment="Center" HorizontalAlignment="Left" Margin="10,16,0,16" Height="32" Width="153">
                <TextBlock x:Name="duration" Text="00:00" Foreground="White" VerticalAlignment="Center"/>
                <Image Source="../assets/volume.png" Stretch="None" Margin="10, 0, 0, 0" VerticalAlignment="Center"/>
                <Image Source="../assets/fullScreen.png" Stretch="None" Margin="10, 0, 0, 0" VerticalAlignment="Center" Tapped="fullscreenSwitch_tapped"/>
            </StackPanel>
        </Grid>

    </Grid>
</Page>
