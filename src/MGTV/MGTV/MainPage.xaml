<Page
    x:Class="MGTV.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="using:MGTV.Controls"
    xmlns:local="using:MGTV"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Style="{StaticResource RootGrid}">
        <Grid.RowDefinitions>
            <RowDefinition Height="128"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid x:Name="header" Grid.Row="0" VerticalAlignment="Top" Margin="100, 50, 50, 0">
            <Image Source="assets/tvlogo.png" Stretch="None" VerticalAlignment="Center" HorizontalAlignment="Left"/>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center">
                <Image Source="assets/search.png" Stretch="None"/>
                <Image Source="assets/history.png" Margin="15, 0, 0, 0" Stretch="None"/>
            </StackPanel>
        </Grid>

        <Grid x:Name="content" Grid.Row="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="588"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            
            <ScrollViewer IsHitTestVisible="True"
                ScrollViewer.IsScrollInertiaEnabled="True" 
                ScrollViewer.HorizontalScrollBarVisibility="Hidden"
                ScrollViewer.VerticalScrollBarVisibility="Hidden"
                ScrollViewer.HorizontalScrollMode="Enabled"
                ScrollViewer.VerticalScrollMode="Disabled"
                ScrollViewer.ZoomMode="Disabled">
                <StackPanel Orientation="Horizontal" Margin="300, 0">
                    <controls:HotRecommendation HorizontalAlignment="Left" Margin="-200, 0, 0, 0"/>
                    
                    <controls:Category CategoryName="综艺" Margin="100, 0, 0, 0"/>

                    <controls:Category CategoryName="连续剧" Margin="100, 0, 0, 0"/>

                    <controls:Category CategoryName="电影" Margin="100, 0, 0, 0"/>
                </StackPanel>
            </ScrollViewer>
        </Grid>
    </Grid>
</Page>
